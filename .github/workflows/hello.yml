name: Hello Cron

on:
  workflow_dispatch:         # 手動実行ができる
  schedule:
    - cron: "*/5 * * * *"   # 5分ごと（UTC基準）

jobs:
  say-hello:
    runs-on: ubuntu-latest
    steps:
      - name: Print Message
        run: echo "Hello from GitHub Actions at $(date)"
