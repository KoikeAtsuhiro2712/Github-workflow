
name: Hello Cron

on:
  schedule:
    - cron: "/5 * * * *"

jobs:
  say-hello:
    runs-on: ubuntu-latest
    steps:
      -name: Print Message
       run: echo "Hello from GitHub Actions at $(date)"
